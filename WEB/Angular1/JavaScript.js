angular.module('myApp')
.controller('body', function ($scope, $rootScope, $http, $compile, $window) {
    

    // directives
    //var el = '<div id="' + id + '" ' + directive + '></div>';
    //var element = angular.element(document.querySelector('#' + holderDiv));
    //var generated = element.html(el);
    //$compile(generated.contents())($scope); refresh the Angular elements
});